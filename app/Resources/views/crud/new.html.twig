{% extends 'base.html.twig' %}

{% block body %}
    <h1>Invoice creation</h1>

    <div class="row">
        <div class="col-sm-5">
            <div class="well">
                {{ form_start(form) }}
                <div class="form-group">
                    {{ form_widget(form) }}
                </div>
                    <input type="submit" value="Create" />
                {{ form_end(form) }}
            </div>
        </div>
    </div>

    <ul>
        <li  class="list-group-item">
            <a href="{{ path('crud_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
